# sync app server

description "annotation/bookmarks synchronization app service"
author "Christopher Nelson <nadiasvertex@gmail.com>"

# When to start the service
start on runlevel [2345]

# When to stop the service
stop on runlevel [016]

# Automatically restart process if crashed
respawn

# Start the process
exec /usr/local/bin/uwsgi --chdir=/home/vagrant/app --uid=vagrant --wsgi-file app.py --die-on-term --master --processes 2 --http-socket :8080 --stats :9191
